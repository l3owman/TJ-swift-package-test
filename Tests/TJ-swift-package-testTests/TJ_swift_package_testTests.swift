import XCTest
@testable import TJ_swift_package_test

final class TJ_swift_package_testTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TJ_swift_package_test().text, "Hello, World!")
    }
}
